diff --git c/libgalois/src/HWTopoLinux.cpp i/libgalois/src/HWTopoLinux.cpp
index 0835b0070..c34d2db8a 100644
--- c/libgalois/src/HWTopoLinux.cpp
+++ i/libgalois/src/HWTopoLinux.cpp
@@ -26,12 +26,14 @@
 #include <array>
 #include <cassert>
 #include <cerrno>
+#include <cstdio>
 #include <cstring>
 #include <fstream>
 #include <functional>
 #include <memory>
 #include <mutex>
 #include <set>
+#include <string>
 
 #ifdef GALOIS_USE_NUMA
 #include <numa.h>
@@ -101,7 +103,7 @@ std::vector<cpuinfo> parseCPUInfo() {
   std::vector<cpuinfo> vals;
 
   const int len = 1024;
-  std::array<char, len> line;
+  std::string line;
 
   std::ifstream procInfo("/proc/cpuinfo");
   if (!procInfo)
@@ -109,8 +111,7 @@ std::vector<cpuinfo> parseCPUInfo() {
 
   int cur = -1;
 
-  while (true) {
-    procInfo.getline(line.data(), len);
+  while (std::getline(procInfo, line)) {
     if (!procInfo)
       break;
 
